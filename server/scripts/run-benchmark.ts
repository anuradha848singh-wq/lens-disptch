import { fetchRealNews } from '../news-fetcher.js'; import { benchmarkMetrics } from '../benchmark-collector.js'; import { db } from '../db.js'; import { publishers } from '../../shared/schema.js'; async function run() { console.log('Clearing lastFetchedAt for all publishers...'); await db.update(publishers).set({ lastFetchedAt: null }); console.log('Running benchmark...'); await fetchRealNews(); console.log('Benchmark data:'); console.table(benchmarkMetrics.data.map(m => ({ Stage: m.stage, Status: m.status, DurationMs: m.durationMs }))); process.exit(0); }; run().catch(console.error);